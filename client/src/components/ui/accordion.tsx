import { forwardRef, HTMLAttributes, InputHTMLAttributes, LabelHTMLAttributes, ButtonHTMLAttributes, ElementRef, ComponentPropsWithoutRef, ReactNode } from "react"

// import * as AccordionPrimitive from "@radix-ui/react-accordion"
import { ChevronDown } from "lucide-react"

import { cn } from "@/lib/utils"

// Accordion component commented out due to missing @radix-ui/react-accordion package
// const Accordion = AccordionPrimitive.Root
// ... rest of accordion code commented out since it's not used in the main app

export { /* Accordion, AccordionItem, AccordionTrigger, AccordionContent */ }
